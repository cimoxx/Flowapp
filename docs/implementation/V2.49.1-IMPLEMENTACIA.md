# Flow v2.49.1 – Backup fix

- Cloud backup používa `Spreadsheet.copy(name)` namiesto `DriveApp.makeCopy`.
- Nie je potrebné extra DriveApp oprávnenie.
- `data_health` je read-only a nevolá `getPlanningData_`.
- UI zobrazuje presnú backend chybu.
- Finančné výpočty sa nemenia.
